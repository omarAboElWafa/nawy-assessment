export abstract class BaseRepository<T> {
  // This is a generic class that will be used to create repositories for different models(ORMs or ODMs).
  // This class will have methods that will be common for all repositories.
}
